import React from 'react';
import { Card } from '~/shared/components';
import './Work.scss';

const Work: React.FC = () => {
  return (
    <Card className="work">
      <Card.Title>Work</Card.Title>
    </Card>
  );
};

export default Work;
